<% @reports.each_slice(3) do |slice| %>
  <div class="row-fluid">
    <% slice.each do |r| %>
      <div class="query_result_row span4">
        <%= render :partial => 'reports/card_small', :locals => { :report => r } %>
      </div>
    <% end %>
  </div>
<% end %>

