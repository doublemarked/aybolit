$("#results_container").append("<%=j render :partial => "card_small", :collection => @hospitals, :as => :hospital %>");
<% unless @update %>
  store_criteria( <%=raw({ :location => @location, :latitude => @latitude, :longitude => @longitude, :sorting => @sorting, :offset => @offset }.to_json) %> );
<% end %>
has_more_results(<%= @more_results.to_json %>);
